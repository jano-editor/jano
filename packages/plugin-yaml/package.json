{
  "name": "@jano-editor/plugin-yaml",
  "version": "1.0.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --platform=node --outfile=dist/index.js",
    "install-plugin": "pnpm build && node scripts/install.js"
  },
  "devDependencies": {
    "@jano-editor/plugin-types": "workspace:*",
    "esbuild": "^0.24.0"
  }
}
